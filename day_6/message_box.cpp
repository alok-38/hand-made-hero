#include <windows.h>

int main()
{
    // MessageBox(NULL, TEXT("Hello from C++ (x64)!"), TEXT("MessageBox Example"), MB_OK | MB_ICONINFORMATION);
    MessageBox(NULL, TEXT("Hello world!"), TEXT("Info"), MB_OK);

    return 0;
}
